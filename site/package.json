{
  "name": "telnyx-agent-portal-site",
  "version": "0.1.0",
  "description": "Local dev server for agent.telnyx.com — content negotiation, agent.json, branded HTML",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
